cmake_minimum_required(VERSION 3.14)
project(C-Utils LANGUAGES C)
add_library(cutils INTERFACE)
target_include_directories(cutils INTERFACE $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>)
add_library(CUtils::cutils ALIAS cutils)
